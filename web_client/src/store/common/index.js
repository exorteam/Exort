import currentUser from './current-user'
import associationSelector from './association-selector'

export default {
    namespaced: true,
    modules: {
        currentUser,
        associationSelector
    }
}
